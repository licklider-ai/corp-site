# Welch ANOVA upstream-report publication

Publication date: September 10, 2026.

Public evidence: https://github.com/scipy/scipy/issues/26146

The issue body, code, warning messages and environment were checked against the
prepared submission. At publication preparation, the issue was open with zero
comments. The defect label is not maintainer confirmation. The article describes
SciPy 1.18.1, NumPy 2.3.5 and Python 3.12.14 on Linux x86_64, with deliberately
extreme lossless scaling, finite individual weights and an overflowing weight sum.
It makes no prevalence estimate, general patch claim, or additional product claim.

One Engineering / Upstream report entry supplies Latest, the Engineering index,
home Latest, RSS and JSON Feed. Adding the distinct report makes eight total
reports while the three previously recorded matching merged fixes stay unchanged;
other upstream dispositions have not been re-audited. About and llms.txt consume
the shared report data. The home carousel retains its existing first-seven
selection and full-width compact layout. The R Wilcoxon report stays in the full
catalog and About, beyond the first seven home panels. Repeated carousel copies
are visual duplicates, not additional reports.

The article, metadata and panel copy were prepared with AI assistance from the
reporter's published issue and reviewed submission evidence. This publication
record is not an independent scientific review or maintainer acceptance.

Implementation starts from the completed carousel revision 4d914edcae21570e78c1a71e398e4845b778ce69.
The homepage change outside shared data is its dateModified value. No carousel
markup, styles, animation, controls, or accessibility behavior is changed.

Validation: the configured Astro check/build commands completed with zero errors,
warnings or hints and 52 generated pages. Generated HTML checks cover the article
code, table, canonical URL, feeds, sitemap, About, llms.txt, report totals and seven
panels with an aria-hidden non-focusable duplicate group. Browser screenshot QA
was not performed because the browser executable could not be downloaded in the
working environment. Carousel layout/style files are unchanged.
